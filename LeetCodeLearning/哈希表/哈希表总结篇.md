## 哈希表总结篇

## 哈希表特性：

常用的哈希表数据结构：

1. Set，特点是集合元素不重复，用在某些特定的去重场景
2. Map，作用场景比较万能，因为有<key,value>的存储结构，使得map几乎可以全场景覆盖，但是有时为了优化程序，不需要用到map

## 典型应用：

1. 针对Set的应用，比如349.两个数组的交集
2. 针对Map的应用，比如1.两数之和，454.四数相加
3. Map独立解决难题的场景可能还是比较有限，大多数涉及到哈希表的题目可能也会另加考察其他算法，在合适的场景选择对应的哈希表是运用关键。